fn main() {

    //Exercise 1
    let x: i32; // If variable is initialised but used, it will error
    let y: i32; // If variable is initialised but unused, it will return a warning
    // To bypass the warning, use an _
    let _z: i32
    // Assert equality macro, which will error if the equality is not met
    assert_eq!(x, 5)
    println!("Success!")


}