#[allow(unused_variables)]
fn main() {
    // This will output a warning
    // Easiest solution is to apend an _ to the start of the variable name => _x
    // Another solution is to use #[allow(unused_variables)] at the top of the script
    let x = 1
}